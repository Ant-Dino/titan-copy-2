import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'; // assuming this file is named App.tsx and is in the same directory

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);